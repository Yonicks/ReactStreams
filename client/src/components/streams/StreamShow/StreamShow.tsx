import * as React from "react";

export interface Props {
    children?: React.ReactNode
}

export interface State {
}

const StreamShow = (props: Props, state: State) => (
    <div>StreamShow</div>
)



export default StreamShow;