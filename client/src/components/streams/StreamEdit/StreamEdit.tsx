import * as React from "react";

export interface Props {
    children?: React.ReactNode
}

export interface State {
}

const StreamEdit = (props: Props, state: State) => (
    <div>StreamEdit</div>
)


export default StreamEdit;