import * as React from "react";

export interface Props {
    children?: React.ReactNode
}

export interface State {
}

const StreamList = (props: Props, state: State) => (
    <div>StreamList</div>
)



export default StreamList;